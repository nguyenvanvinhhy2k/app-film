export const SET_USER = (state, user) => {
  state.user = user;
};

export const SET_USER_ID = (state, userID) => {
  state.userID = userID;
};

export const SET_PHOTO_URL = (state, url) => {
  state.photoURL = url;
};
